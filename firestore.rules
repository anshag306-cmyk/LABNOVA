rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read, write for lab management collections
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
